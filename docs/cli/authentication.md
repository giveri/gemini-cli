## Authentication Setup

Gemini CLI uses a local Ollama API and does not require any credentials. Start the Ollama server locally (by default it listens on `http://localhost:11434`). Optionally set `OLLAMA_BASE_URL` to point to a different host or port.
